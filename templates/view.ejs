package <%= path %>;

import android.graphics.Canvas;
import android.view.View;

public class <%= name %>View extends View {
	
	<%= name %>Activity activity;
	<%= name %>TouchListener touch;

	public <%= name %>View(<%= name %>Activity activity){
		super(activity);
		this.activity = activity;

		touch = new <%= name %>TouchListener(this);

		setOnTouchListener(touch);
	}

	@Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }

}