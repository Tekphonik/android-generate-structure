package <%= path %>;

import android.app.Activity;
import android.os.Bundle;

public class <%= name %>Activity extends Activity {

	<%= name %>View view;

	@Override
	protected void onCreate(Bundle savedInstanceState){
		super.onCreate(savedInstanceState);

		view = new <%= name %>View(this);

		setContentView(view);
	}
}